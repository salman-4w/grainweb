<%= stateful_ui current_user, :contract_bookings_grid %>

<%= render_grid url: contract_bookings_path(@contract),
                klass: Booking,
                size: "780x440",
                html: {id: "contract_bookings_grid"},
                print_button: true,
                search_bar: true
-%>

<script type="text/javascript" charset="utf-8">
resizeGrid(grid_for_contract_bookings_grid, <%= @contract.id %>);
</script>
